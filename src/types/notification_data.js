'use strict'

/**
 * Notification data, passed to notifier
 *
 * @typedef {object} NotificationData
 * @property {string} title - notification title
 * @property {string|string[]} message - notification body passed to `sprintf`
 *   for rendering, such may be array of strings
 */
