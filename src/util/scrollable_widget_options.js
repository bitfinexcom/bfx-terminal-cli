'use strict'

const scrollableWidgetOptions = {
  mouse: true,
  scrollable: true,
  scrollbar: {
    ch: '|',
    track: { bg: 'black' }
  },

  style: {
    scrollbar: { fg: 'green' }
  }
}

module.exports = scrollableWidgetOptions
